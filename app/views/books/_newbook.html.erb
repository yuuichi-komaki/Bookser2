<h2>New book</h2>
 <% if book.errors.any? %>
        <%= book.errors.count %>error prohibited this book from being saved:
        <% book.errors.full_messages.each do |message| %>
          <%= message %>
        <% end %>
      <% end %>

<%= form_with model: book, local:true do |f| %>

  <h5>Title</h5>
  <%= f.text_field :title %>

  <h5>Opinion</h5>
  <%= f.text_area :body %>

  <p><%= f.submit 'Create Book', class:"btn-sm btn-success" %></p>

<% end %>